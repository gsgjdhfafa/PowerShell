services:
  bot:
    build: .
    container_name: zf-bot
    restart: unless-stopped
    env_file: .env
    logging:
      driver: json-file
      options:
        max-size: "5m"
        max-file: "3"
